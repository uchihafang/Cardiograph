<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <TextView
        android:id="@+id/editView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/btnUserName"
        android:text="Pulse"
        android:textSize="40dp"
        tools:context=".MainActivity" />
    
    <Button
        android:id="@+id/btnStop"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/progressBar"
        android:layout_toRightOf="@+id/btnStart"
        android:layout_margin="5dp"
        android:onClick="onBtnStopClick"
        android:enabled="false"
        android:text="Stop" />

    <Button
        android:id="@+id/btnStart"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/progressBar"
        android:layout_margin="5dp"
        android:onClick="onBtnStartClick"
        android:text="Start" />

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="fill_parent"
        android:layout_height="100dp"
        android:layout_below="@+id/btnUserName"
        android:layout_margin="5dp"
        android:layout_marginTop="0dp"
        android:maxHeight="50dip"
        android:maxWidth="1000dip" />

    <Button
        android:id="@+id/btnUserName"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:onClick="onBtnUserNameClick"
        android:text="@string/btnUserNameText" />

</RelativeLayout>
